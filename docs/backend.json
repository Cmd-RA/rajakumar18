
{
  "entities": {
    "User": {
      "title": "User",
      "type": "object",
      "description": "Represents a user account and their personal channel.",
      "properties": {
        "id": { "type": "string" },
        "username": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "isAdmin": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "username", "email", "isAdmin"]
    },
    "Post": {
      "title": "Post",
      "type": "object",
      "description": "Represents a photo post.",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "photoUrl": { "type": "string" },
        "caption": { "type": "string" },
        "likesCount": { "type": "number" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "userId", "photoUrl", "caption", "likesCount", "createdAt"]
    },
    "SiteSettings": {
      "title": "SiteSettings",
      "type": "object",
      "description": "Global site configurations like AdSense code and Meta tags.",
      "properties": {
        "adsenseCode": { "type": "string", "description": "The script or meta tag provided by Google AdSense." },
        "updatedAt": { "type": "string", "format": "date-time" }
      }
    }
  },
  "auth": {
    "providers": ["google", "password"]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "description": "Public user profiles."
        }
      },
      {
        "path": "/posts/{postId}",
        "definition": {
          "entityName": "Post",
          "description": "Public photo posts."
        }
      },
      {
        "path": "/settings/site_config",
        "definition": {
          "entityName": "SiteSettings",
          "description": "Global settings for SEO and AdSense. Only admin can write."
        }
      },
      {
        "path": "/roles_admin/{userId}",
        "definition": {
          "entityName": "User",
          "description": "Admin role assignment collection."
        }
      }
    ]
  }
}
